filter:
    excluded_paths:
        - 'tests/*'
        - 'features/*'

checks:
    php: true

build:
    environment:
        php:
            version: 7.1.3
    tests:
        override:
            -
                command: vendor/bin/simple-phpunit --coverage-clover=clover.xml
                coverage:
                    file: clover.xml
                    format: php-clover
